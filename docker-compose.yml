version: "3"
services:
  ansibleawx:
    build: ansible/.
    container_name: ansibleawx
    volumes:
      - ./ansible/projects:/var/lib/awx/projects:rw
    privileged: true
    ports:
      - "8081:80"
  raspi:
    build: raspi/.
    container_name: raspi
    ports:
      - "20022:22"
